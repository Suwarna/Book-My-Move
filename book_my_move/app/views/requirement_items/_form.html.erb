<%= form_for(@requirement_item) do |f| %>
  <% if @requirement_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@requirement_item.errors.count, "error") %> prohibited this requirement_item from being saved:</h2>

      <ul>
      <% @requirement_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :requirement_id %><br>
    <%= f.text_field :requirement_id %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.text_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
