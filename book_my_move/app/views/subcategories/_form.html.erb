<%= form_for(@subcategory) do |f| %>
  <% if @subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
      <% @subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %><br>
    
<%= f.collection_select :category_id, Category.all, :category_id ,:category_name  %>


  </div>
  <div class="field">
    <%= f.label :subcategory_name %><br>
   <%= f.text_field :subcategory_name %> *Make this text field to auto-complete refer ->  

 <%= f.select :subcategory_name, Subcategory.all.map { |e| [e.subcategory_name] }.uniq! %> 


  </div>
  <div class="field">
    <%= f.label :subcategory_desc %><br>
    <%= f.text_field :subcategory_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
